
import 'package:flutter/material.dart';

class busTrack extends StatelessWidget {
  const busTrack({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('bus Track'),
      ),
    );
  }
}
